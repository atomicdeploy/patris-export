{
  "name": "patris-export-web",
  "version": "1.0.0",
  "description": "Modern SPA for Patris Export data visualization",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "sass": "^1.101.0",
    "vazirmatn": "^33.0.3"
  }
}
